import {Button, Header, UsersList} from "@/components/Components.tsx";

export const Home = () => {
    return (
        <div>
            <Header />
            <Button />
            <UsersList />
        </div>
    );
};

